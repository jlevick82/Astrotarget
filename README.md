# Astrotarget Landing Page

This is a static landing page that links to the tools under `./Go-No-Go/`.
Drop these files at the **repo root** of `jlevick82/Astrotarget`.

- `index.html` (this page)
- `styles.css`
- `/assets/logo-*.png|webp`
- `favicon.ico`
